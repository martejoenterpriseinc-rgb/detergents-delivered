import { PhasePlaceholder } from "@/components/admin/phase-placeholder";

export default function Page() {
  return (
    <PhasePlaceholder
      title="Vendors"
      phase={4}
      summary="Vendor and vendor-product mappings are designed; admin screens are later."
    />
  );
}
